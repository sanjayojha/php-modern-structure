<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ pageTitle }}</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <h1>{{ pageTitle }}</h1>
        <nav>
            <a href="/">Home</a> |
            <a href="/about">About</a> |
            <a href="/hello/world">Hello World</a>
        </nav>
    </header>
    <main>
        <p>{{ message }}</p>

        <h2>Users:</h2>
        {% if users is not empty %}
            <ul>
                {% for user in users %}
                    <li>
                        <a href="/user/{{ user.id }}">
                            {{ user.name }} ({{ user.email }}) - Joined: {{ user.createdAt }}
                        </a>
                    </li>
                {% endfor %}
            </ul>
        {% else %}
            <p>No users found.</p>
        {% endif %}
    </main>
    <footer>
        <p>&copy; 2025 Modern PHP App</p>
    </footer>
</body>
</html>